// import { api } from '$lib/api';
//
// const cookies = document.cookie.split('; ') ?? [];
// const session = cookies.filter(c => c.startsWith('auth_token='))[0]?.split('=')[1];
//
// api.setToken(session)
